{
  "name": "legion-marketing-factory",
  "private": true,
  "workspaces": [
    "renderer",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/run-package-script.js",
    "build": "node scripts/run-package-script.js",
    "generate": "npm run generate -w renderer --"
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
